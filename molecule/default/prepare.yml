---
- name: Prepare
  hosts: all
  gather_facts: false
  tasks:
    # - name: Prepare system RedHat (Oracle Linux)
    #   when: ansible_os_family == 'RedHat'
    #   raw: dnf install -y python3 python3-pip
    #   changed_when: false

    - name: Prepare system Debian (Ubuntu)
      when: ansible_os_family == 'Debian'
      raw: apt-get update && apt-get install -y python3 python3-pip
      changed_when: false
